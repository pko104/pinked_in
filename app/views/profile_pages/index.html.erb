<!DOCTYPE html>
<html>
<head>
    <meta name="viewport" content="width=device-width,user-scalable=no">
    <link rel="stylesheet" type="text/css" href="profile_pages.scss">
</head>
  <nav>
  <ul>
      <div id="signUp">
        <div><img id="logo" src="assets/logo.png"></div>
        <div id="logIn">
          <%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
            <li>
              <div class="field">
                <%= f.label :email, "Email Address" %></br>
                <%= f.email_field :email, autofocus: true %>
              </div>
            </li>
            <li>
              <div class="field-2">
                <%= f.label :password %></br>
                <%= f.password_field :password, autocomplete: "off" %>
              </div>
            </li>
            <li>
              <div class="field-3">
                <%= f.submit "Sign in" %>
              </div>
            </li>
          <% end %>
         </div>
      </div>
      </ul>
  </nav>
<body>

<div id="centerBox">
    <h1 class="title">Be great at what you do</h1>
    <h2 class="title">Get started - it's free.</h2>
    <%= form_for(resource, :as => resource_name, :url => registration_path(resource_name)) do |f| %>
      <%= devise_error_messages! %>
      <div>
        <h3 class ="text-field">First Name</h3>
        <%= f.text_field :first_name,:size => "45x30"%>
      </div>
      <div>
        <h3 class ="text-field">Last Name</h3>
        <%= f.text_field :last_name, :size => "30x30"%>
      </div>
      <div>
        <h3 class ="text-field">Email</h3>
        <%= f.email_field :email, :size => "60x30"%>
      </div>
      <div>
        <h3 class ="text-field">Password (6 or more characters)</h3>
        <%= f.password_field :password, :size => "60x30"%>
      </div>
      <div class="disclaimer"><br />
        <%= "By clicking Join Now, you agree to LinkedIn's User Agreement, Privacy Policy and Cookie Policy."%>
      </div>
      <div class ="box"><br />
        <%= f.submit "Join now" %>
      </div>
    <% end %>
</div>

</body>
</html>

